create database dbgladson;
use dbgladson;

ALTER DATABASE dbgladson DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci; 

CREATE TABLE pessoas (id int auto_increment primary key,
   nome varchar(20) not null,
   sobrenome varchar(20) not null,
   cpf varchar(30) not null,
   rg varchar (20) not null,
   cep varchar(20) not null,
   endereco varchar(20)
);

 # teste para inserir informação
-- insert into pessoas (nome, sobrenome, cpf, rg, cep, endereco) values ("Gladson","Simoes","234.345.678-22","4567-345","45667-89","Minha casa");
 
 # Comando para editar pessoa pelo id 
-- UPDATE pessoas SET  nome = "" , sobrenome = "" , cpf = "" , rg = "" , cep = "", endereco ="" WHERE id = ;
 
 # Comando para deletar pessoa pelo o id
 -- DELETE FROM pessoas WHERE id = ;
 
select * from dbgladson.pessoas;


